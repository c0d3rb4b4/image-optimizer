# Image Optimizer Configuration
# Only tuneable settings - internal paths are hardcoded in the application

# Image processing settings
TARGET_WIDTH=2560
TARGET_HEIGHT=1440
JPEG_QUALITY=95

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
